// add whatever parameters you deem necessary
function averagePair(arr, target) {
   for (let i = 0 ; i <arr.length; i++)
   { if (arr[0] + arr[i]/ 2 === target)
        {return true}
    else{
        return false
    }
}}
